#!/bin/bash
# AI Task Agent - Simple wrapper script
# Usage: ./ai <task> <text>

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

cd "$SCRIPT_DIR"
source venv/bin/activate 2>/dev/null
python cli.py "$@"
